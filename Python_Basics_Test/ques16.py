#Write a Python program to calculate the percentage increase in a product's price.
old_pro_price=399
new_pro_price=599
inc_per= (new_pro_price - old_pro_price ) / 100

print(f'The percentage increase in products price is : {inc_per:}%')
